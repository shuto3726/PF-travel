<div class="container mt-3">
  <div class="row">
    <div class="col-12">
      <h2>"<%= @user.name %>" フォロワー一覧</h2>
      <% if @users.exists? %>
        <%= render '/users/index', users: @users %>
      <% else %>
        <p>ユーザーはいません</p>
      <% end %>
    </div>
  </div>
</div>